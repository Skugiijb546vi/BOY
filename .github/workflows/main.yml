name: Extract_Audio_From_Telegram_Private
on:
  workflow_dispatch:
    inputs:
      tg_url:
        description: 'لینکی پۆستی تێلیگرام (نموونە: https://t.me/Movie_Timee1/2661)'
        required: true
      audio_name:
        description: 'ناوی فایلە دەنگییەکە لە Hugging Face (بە .mp3 کۆتایی بێت)'
        required: true
        default: 'movie_audio.mp3'

jobs:
  extract_and_upload:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗️ Setup Tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg -qq
          python -m pip install -U pip pyrogram tgcrypto huggingface_hub

      - name: ⬇️ Download Video via Pyrogram
        env:
          TG_API_ID: ${{ secrets.TG_API_ID }}
          TG_API_HASH: ${{ secrets.TG_API_HASH }}
          TG_SESSION: ${{ secrets.TG_SESSION }}
          TG_URL: ${{ github.event.inputs.tg_url }}
        run: |
          python3 -c "
          import os, sys
          from pyrogram import Client
          
          api_id = os.environ.get('TG_API_ID')
          api_hash = os.environ.get('TG_API_HASH')
          session_string = os.environ.get('TG_SESSION')
          url = os.environ.get('TG_URL')
          
          if not all([api_id, api_hash, session_string]):
              print('❌ هەڵە: زانیارییەکانی تێلیگرام لە Secrets نەدۆزرانەوە!')
              sys.exit(1)
              
          # جیاکردنەوەی ئایدی کەناڵ و پۆست لە لینکەکەوە
          parts = url.rstrip('/').split('/')
          if 'c' in parts:
              # بۆ کەناڵی تایبەت (Private)
              chat_id = int('-100' + parts[-2])
          else:
              # بۆ کەناڵی گشتی (Public)
              chat_id = parts[-2]
          msg_id = int(parts[-1].split('?')[0])
          
          app = Client('my_account', api_id=int(api_id), api_hash=api_hash, session_string=session_string)
          
          async def main():
              async with app:
                  print(f'🔍 دەگەڕێم بەدوای نامەی ژمارە {msg_id} لە {chat_id}...')
                  msg = await app.get_messages(chat_id, msg_id)
                  
                  if not msg or (not msg.video and not msg.document):
                      print('❌ هیچ ڤیدیۆیەک لەم پۆستەدا نییە!')
                      sys.exit(1)
                      
                  print('📥 خەریکی داگرتنی ڤیدیۆکەیە (ئەمە چەند خولەکێک دەخایەنێت)...')
                  await app.download_media(msg, file_name='video.mp4')
                  print('✅ ڤیدیۆکە بە سەرکەوتوویی داگیرا.')
          
          app.run(main())
          "

      - name: 🎵 Extract Audio & Delete Video
        run: |
          echo "خەریکی جیاکردنەوەی دەنگەکەیە لە ڕێگەی FFmpeg..."
          ffmpeg -i "downloads/video.mp4" -q:a 0 -map a "${{ github.event.inputs.audio_name }}"
          echo "✅ دەنگەکە جیاکرایەوە."
          echo "🗑️ سڕینەوەی ڤیدیۆ گەورەکە بۆ بەتاڵکردنی شوێن..."
          rm -rf downloads/video.mp4

      - name: 🚀 Upload Audio to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          python3 -c "
          import os, sys
          from huggingface_hub import HfApi
          
          token = os.getenv('HF_TOKEN')
          audio_file = '${{ github.event.inputs.audio_name }}'
          
          if not os.path.exists(audio_file):
              print('❌ هەڵە: فایلە دەنگییەکە نەدۆزرایەوە!')
              sys.exit(1)
              
          api = HfApi()
          repo_id = 'Sarkoakram/sarko761'
          
          print(f'📤 ناردنی فایل بۆ Hugging Face: {audio_file}...')
          api.upload_file(
              path_or_fileobj=audio_file,
              path_in_repo=audio_file,
              repo_id=repo_id,
              repo_type='dataset',
              token=token
          )
          print('✅ سەد لە سەد سەرکەوتوو بوو!')
          "
